# 项目全局强制规范【最高优先级，任何代码修改必须遵守】

项目名称：世界观档案馆（小说资料库系统）
分为两部分：Netlify静态网页端、本地手机App客户端

## 一、核心架构铁律（永久禁止改动）

1. 数据流单向规则
网页端**仅允许导入资料库文件，彻底移除所有导出完整JSON数据包功能**。
网页只作为游客浏览载体；完整原始资料唯一可信来源：绑定机器码的本地手机App。

2. 密钥/私有盐安全红线【绝对不能违反】
- 原始私有盐仅允许硬编码保存在【本地手机App】内部；
- **严禁以任何形式（明文、变量、注释、配置）将私有盐打包、写入网页前端所有代码**；
- 静态网页不储存任何密钥、盐值，网页只保留哈希运算公式；
- 网页解锁依靠用户手动输入固定密码参与签名校验。

## 二、文件导入&签名校验标准流程

1. 本地App导出流程
资料库全部内容data + 私有盐 → 哈希运算，生成sign签名字段，嵌入最终JSON文件。

2. 网页导入校验流程
上传文件后优先JSON.parse读取sign（仅读取签名，不加载业务数据）；
输入管理员密码，使用【密码 + JSON内data数据】进行哈希计算；
计算结果和文件内置sign匹配成功，才加载资料；
校验失败 → 直接终止、丢弃全部文件数据，不解析、不储存文件内容。

3. 禁止方案
不实现动态时效PIN码、时间相关验证码、虎符式分离双密钥；
避免静态站点两端环境差异、时间不同步导致校验失败，防止本人无法解锁系统。

## 三、网页功能规范

1. 游客模式：默认开放，正常浏览角色、势力、时间轴、长篇小说文本；
2. 管理员入口：【关于页面标题连续点击5次】唤起密码弹窗，无其他入口；
3. 小说编辑器优化要求：支持几十万字长文本稳定渲染，优化滚动、排版，解决卡顿、文字错乱、页面崩溃。

## 四、代码构建与部署强制检查项

每次代码修改、打包构建完成后，**必须检索打包后的最终网页产物**：
确认私有盐字符串不存在于产物内，才可推送仓库、触发Netlify部署。

## 五、开发通用要求

1. 校验逻辑尽量精简，减少多层嵌套运算；统一两端哈希算法、文本编码格式，规避App与网页运算结果不一致bug；
2. 禁止为调试便利，临时放开安全限制；调试结束必须复原全部安全策略；
3. 任何大规模重构前，先通读本规范，不得违背下述所有条款。

## 六、后续开发约定

若会话上下文信息缺失，优先完整扫描仓库源码 + 阅读本文件，严格按照规范开发；
如功能方案和本文档冲突，**以本文件规则为第一标准**。
